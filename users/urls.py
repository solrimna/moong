from django.urls import path
from . import views

app_name = 'users'

urlpatterns = [
    # 나중에 로그인/회원가입 등 추가
    # path('login/', views.login, name='login'),
    # path('signup/', views.signup, name='signup'),
]